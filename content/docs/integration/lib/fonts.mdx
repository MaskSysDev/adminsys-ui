---
title: Fonts
description: Coleção de fontes recomendadas para uso com a aplicação.
---

## Next Fonts

<Steps>
  <Step>
    ### Fonts

    ```ts title="src/lib/fonts.ts"
    import { Geist, Geist_Mono } from "next/font/google"

    /**
     * Configuração da fonte Geist Sans para uso no Next.js.
     * Define a variável CSS para a fonte e os subconjuntos de caracteres.
     */
    export const geistSans = Geist({
      variable: "--font-geist-sans",
      subsets: ["latin"],
    })

    /**
     * Configuração da fonte Geist Mono para uso no Next.js.
     * Define a variável CSS para a fonte e os subconjuntos de caracteres.
     */
    export const geistMono = Geist_Mono({
      variable: "--font-geist-mono",
      subsets: ["latin"],
    })

    ```

  </Step>
</Steps>

## Usage

```tsx title="src/app/layout.tsx"
import type { Metadata } from "next"

import { geistMono, geistSans } from "@/lib/fonts"// [!code ++]

import "@/styles/globals.css"

export const metadata: Metadata = {
  title: "NextJS 15 Started",
  description: "Generated by create next app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html
      className="scroll-smooth"
      data-scroll-behavior="smooth"
      dir="ltr"
      lang="en"
    >
      <body
      // [!code ++]
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  )
}

```
